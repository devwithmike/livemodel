[project]
name = "livemodel-backend"
version = "0.1.0"
description = "LiveModel Backend API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "uvicorn[standard]>=0.44.0",
]
